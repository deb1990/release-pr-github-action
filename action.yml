name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  GITHUB_TOKEN:
    description: 'github token'
    required: true
outputs:
  time: # id of output
    description: 'The time we greeted you a'
runs:
  using: 'node12'
  main: 'dist/index.js'
